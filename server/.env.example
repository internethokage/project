# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=giftable
DATABASE_USER=giftable
DATABASE_PASSWORD=giftable

# Redis
REDIS_URL=redis://localhost:6379

# Auth
JWT_SECRET=change-this-to-a-secure-secret-in-production

# Server
PORT=3001
CORS_ORIGIN=http://localhost:5173

# AI Suggestions (server-side — optional, enables /api/ai/suggestions)
# Uses OpenAI-compatible API format (works with Ollama, custom endpoints, etc.)
AI_API_KEY=your-api-key-here
AI_BASE_URL=https://your-llm-endpoint/v1
AI_MODEL=your-model-name
